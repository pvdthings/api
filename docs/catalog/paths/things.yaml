paths:
  /things:
    get:
      summary: Returns the data for the catalog app, including things and categories
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object